<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
		<link rel="stylesheet" href="{{ asset('css/login.css') }}">
		<title>SVH - Connexion</title>
	</head>
	<body>

		<a href="{{ path('home') }}" class="d-flex justify-content-center">
			<img src="{{ asset('uploads/svh-logo1.png') }}" height="200px" alt="Logo svh photographie" class="logo-svh text-center">
		</a>
		<div class="container d-flex justify-content-center ">
			<form method="post" name="login" class="rounded shadow-sm">
				
				<h1 class="h3 mb-3 font-weight-normal">Accès admin</h1>
				{% if error %}
				{% set errorMsg = "Identifiant invalide." %}
					<div class="alert alert-danger text-center">{{ errorMsg }}</div>
				{% endif %}
				<label for="inputEmail">Email</label>
				<input type="email" value="" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus placeholder="Adresse email">
				<label for="inputPassword" class="mt-2">Mot de passe</label>
				<input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required placeholder="Mot de passe">

				<input
				type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

				{#
							        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
							        See https://symfony.com/doc/current/security/remember_me.html
							
							        <div class="checkbox mb-3">
							            <label>
							                <input type="checkbox" name="_remember_me"> Remember me
							            </label>
							        </div>
							    #}

                <div class="form-bottom d-flex justify-content-between align-items-center">
				<button class="btn btn-primary mt-4" type="submit">
					Se connecter
				</button>

                <a href="">Mot de passe oublié</a>
                
                </div>
			</form>
		</div>


	</body>
</html>
